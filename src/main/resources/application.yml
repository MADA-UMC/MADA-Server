server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://database-mada.c3o5o9rdofds.ap-northeast-2.rds.amazonaws.com:3306/madaDB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: umc4mada
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher # about swagger